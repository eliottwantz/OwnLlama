import { api } from '.';

export const GET = ({ request }) => api.handle(request);
export const POST = ({ request }) => api.handle(request);
export const PATCH = ({ request }) => api.handle(request);
export const DELETE = ({ request }) => api.handle(request);
export const OPTIONS = ({ request }) => api.handle(request);
